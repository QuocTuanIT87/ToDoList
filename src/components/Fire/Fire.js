import Confetti from 'react-confetti';

const Fireworks = () => {
    return (
        <div style={{ position: 'fixed', left: 0 }}>
            <Confetti />
        </div>
    );
};

export default Fireworks;
